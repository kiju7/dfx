{
  "$schema": "https://json.schemastore.org/claude-code-marketplace.json",
  "name": "kiju7-dfx",
  "owner": {
    "name": "kiju7",
    "url": "https://github.com/kiju7"
  },
  "plugins": [
    {
      "name": "dfx",
      "description": "Multi-agent engineering pipeline that runs natively inside Claude Code (/dfx:dfx → triage → Tech Lead (reads code, decomposes) → parallel devs → 4 QC reviewers → Ralph Loop auto-fix).",
      "source": "./"
    }
  ]
}
